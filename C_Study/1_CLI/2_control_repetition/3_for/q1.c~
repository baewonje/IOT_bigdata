#include <stdio.h>

void main(){
	int minimum valu;
	int maximum valu;
	int total=0;

	printf("최소 값: ");
	scanf("%d", &minimum valu);
	printf("최대 값: ");
	scanf("%d", &maximum valu);

	for(;minimum valu<=maximum valu;minimum valu++){
		total= total+minimum valu;
	}
	printf("총합: %d\n",total);
}
