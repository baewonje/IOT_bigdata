#include <stdio.h>
#pragma warning (disable:4996)

void main(){
		char str[100];
		int idx=0;

		printf("영단어를 입력하세요: ");
		scanf("%s",str);


	if(arr[index] > max){
				max = arr[index];
		}if(arr[index] < min){
				min = arr[index];
		}
	
	printf("가장큰수 %d\n", max);
}

